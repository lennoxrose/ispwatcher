node_modules
# Keep environment variables out of version control
.env

apps/backend/src/generated/prisma
apps/backend/dist
apps/frontend/dist

# Local storage for uploaded protocols + generated complaint PDFs (Layer 2) —
# production uses a mounted PVC instead, see config.ts storage.dataDir
apps/backend/data

# Dont include Ai Files
CLAUDE.md
skills-lock.json
.claude
.agents
.windsurf

# private, not ai-related
tmp.txt
.windsurf/
.claude/